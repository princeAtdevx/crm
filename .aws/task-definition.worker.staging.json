{
	"family": "crm-staging-worker",
	"networkMode": "awsvpc",
	"requiresCompatibilities": ["FARGATE"],
	"cpu": "256",
	"memory": "512",
	"executionRoleArn": "REPLACE_WITH_ECS_TASK_EXECUTION_ROLE_ARN",
	"taskRoleArn": "REPLACE_WITH_ECS_TASK_ROLE_ARN",
	"containerDefinitions": [
		{
			"name": "worker",
			"image": "REPLACE_AT_DEPLOY_TIME",
			"essential": true,
			"command": ["bun", "REPLACE_WITH_WORKER_ENTRYPOINT_ONCE_IT_EXISTS"],
			"environment": [
				{
					"name": "NODE_ENV",
					"value": "production"
				}
			],
			"secrets": [
				{
					"name": "DATABASE_URL",
					"valueFrom": "REPLACE_WITH_STAGING_SECRETS_MANAGER_ARN:DATABASE_URL::"
				},
				{
					"name": "REDIS_URL",
					"valueFrom": "REPLACE_WITH_STAGING_SECRETS_MANAGER_ARN:REDIS_URL::"
				}
			],
			"logConfiguration": {
				"logDriver": "awslogs",
				"options": {
					"awslogs-group": "/ecs/crm-staging-worker",
					"awslogs-region": "REPLACE_WITH_AWS_REGION",
					"awslogs-stream-prefix": "worker"
				}
			}
		}
	]
}
