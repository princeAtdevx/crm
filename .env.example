# Copy to .env and adjust. Loaded by apps/http (via @nestjs/config) and by
# packages/db's drizzle.config.ts — one source of truth for the whole repo.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/crm
PORT=3000
NODE_ENV=development
