// Everything lives in @crm/vitest-config/node: environment, coverage, and the
// ssr.noExternal list that lets Vite transform the raw-TS @crm packages.
export { default } from '@crm/vitest-config/node';
