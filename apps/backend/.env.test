# Committed on purpose: points at a throwaway local database used by the e2e
# suite. Loaded first (ahead of the repo-root .env) when NODE_ENV=test, which
# vitest sets automatically. Put secrets in .env.test.local (gitignored).
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/crm_test
PORT=3001
NODE_ENV=test
